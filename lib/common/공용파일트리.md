c:\workspace_flutter\oathkeeper_front\lib\common\
├── api_response.dart           # CommonResponse<T> 파싱 유틸
├── router.dart                 # 전역 GoRouter + 로그인 리다이렉트 규칙
├── token_interceptor.dart      # Dio 요청에 JWT 추가, 401 처리
├── websocket_service.dart      # STOMP 연결/구독 헬퍼(채팅 room 전용, WS_CHAT_URL 사용)
├── api\
│   ├── ai_api.dart             # AI 전용 Dio(base: AI_BASE_URL, fallback 192.168.0.3:8001)
│   └── endpoints.dart          # Spring/FastAPI 엔드포인트 빌더(API_BASE_URL/AI_BASE_URL 기반)
└── utils\
    └── http_util.dart          # 기본 Dio Provider(API_BASE_URL/IMAGE_BASE_URL 읽음), 토큰/로그 인터셉터
