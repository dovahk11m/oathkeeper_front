# PR 리뷰 반영 리팩토링 문서 (1126-1127)

**작성일**: 2025-11-27
**작업 기간**: 2025-11-26 ~ 2025-11-27
**목적**: PR 리뷰 피드백 반영 및 코드 품질 개선

---

## 📋 목차

1. [PR 리뷰 피드백 요약](#pr-리뷰-피드백-요약)
2. [리팩토링 계획](#리팩토링-계획)
3. [Phase 1: Platform 호환성 확보](#phase-1-platform-호환성-확보)
4. [Phase 2: 에러 핸들링 강화](#phase-2-에러-핸들링-강화)
5. [전체 변경 요약](#전체-변경-요약)
6. [검증 및 테스트](#검증-및-테스트)

---

## PR 리뷰 피드백 요약

2025-11-26에 받은 PR 리뷰에서 5가지 주요 피드백을 받았습니다.

### 🔴 높은 우선순위

#### 1. Platform별 기본 URL 불일치

**위치**: `http_util.dart`, `endpoints.dart`, `live_map_page.dart`
**문제**: localhost vs 10.0.2.2 혼재 사용
**영향**: 안드로이드 에뮬레이터에서 서버 접속 불가

```dart
// 문제 코드
const String _fallbackApi = "http://localhost:8080/api";  // ❌ 안드로이드 X
```

#### 2. TrackUploadResult.fromJson 과도한 관대함

**위치**: `tracking_dto.dart`
**문제**: 잘못된 JSON에도 조용히 기본값(0) 반환
**영향**: 서버 응답 변경 시 디버깅 어려움

```dart
// 문제 코드
return const TrackUploadResult(0);  // ❌ 에러 숨김
```

### 🟡 중간 우선순위

#### 3. 공용 ApiResponse 파서 미사용

**위치**: `tracking_repository.dart`
**문제**: 응답 파싱 로직 중복
**영향**: 유지보수성 저하

#### 4. endpoints.dart replaceAll 불안정성

**위치**: `endpoints.dart`
**문제**: `.env` URL 형식에 대한 가정
**영향**: 설정 값 형식 의존성 높음

### ⚪ 낮은 우선순위

#### 5. .env 구조 재정의

**제안**: HOST만 정의하고 경로는 코드에서 조합
**상태**: 기존 호환성 고려 필요

---

## 리팩토링 계획

### 우선순위 결정

| Phase       | 우선순위 | 작업 내용            | 상태    |
| ----------- | -------- | -------------------- | ------- |
| **Phase 1** | 🔴 높음  | Platform 호환성 확보 | ✅ 완료 |
| **Phase 2** | 🔴 높음  | 에러 핸들링 강화     | ✅ 완료 |
| **Phase 3** | 🟡 중간  | replaceAll 제거      | ✅ 완료 |

### 작업 범위

**Phase 1-3 모두 완료** ✅

---

## Phase 1: Platform 호환성 확보

### 📊 작업 개요

**날짜**: 2025-11-27
**목표**: 안드로이드 에뮬레이터 자동 지원
**결과**: Platform별 동적 URL 설정 구현

### 1️⃣ 신규 파일 생성

#### `lib/common/utils/platform_defaults.dart` ✨

```dart
import 'dart:io';

/// Platform별 기본 URL 제공
///
/// 안드로이드 에뮬레이터는 localhost 대신 10.0.2.2를 사용해야 호스트 PC에 접속 가능
/// iOS 시뮬레이터 및 데스크톱은 localhost 사용
class PlatformDefaults {
  /// 안드로이드 에뮬레이터는 10.0.2.2, 나머지는 localhost
  static String get localhost {
    if (Platform.isAndroid) {
      return '10.0.2.2';
    }
    return 'localhost';
  }

  /// HTTP API 기본 URL (Spring Boot)
  static String get httpApi => 'http://$localhost:8080/api';

  /// 이미지 서버 기본 URL
  static String get httpImage => 'http://$localhost:8080';

  /// WebSocket 기본 URL (채팅)
  static String get wsChat => 'ws://$localhost:8080/ws';

  /// WebSocket 기본 URL (라이브맵 - STOMP)
  static String get wsLiveMap => 'ws://$localhost:8080/ws-stomp';

  /// AI 서버 기본 URL (FastAPI - 실제 네트워크 IP)
  static const String aiBase = 'http://192.168.0.3:8001';
}
```

**핵심 원리**: `Platform.isAndroid`로 런타임 플랫폼 감지

---

### 2️⃣ 기존 파일 수정

#### `lib/common/utils/http_util.dart` 🔧

```dart
// Before
const String _fallbackApi = "http://localhost:8080/api";
const String _fallbackImage = "http://localhost:8080";

// After
import 'platform_defaults.dart';

String get _fallbackApi => PlatformDefaults.httpApi;
String get _fallbackImage => PlatformDefaults.httpImage;
```

**변경 사항**: `const` → `getter`로 변경 (런타임 Platform 감지)

---

#### `lib/common/api/endpoints.dart` 🔧

```dart
// Before
const String _fallbackApi = 'http://localhost:8080';
const String _fallbackAi = 'http://192.168.0.3:8001';

// After
import '../utils/platform_defaults.dart';

String get _fallbackApi => PlatformDefaults.httpImage;
String get _fallbackAi => PlatformDefaults.aiBase;
```

---

#### `lib/view/live_map/live_map_page.dart` 🔧

```dart
// Before
const _wsUrl = String.fromEnvironment('WS_URL', defaultValue: 'ws://localhost:8080/ws');

// After
import 'package:oath_client/common/utils/platform_defaults.dart';

final _wsUrl = String.fromEnvironment(
  'WS_URL',
  defaultValue: PlatformDefaults.wsLiveMap,
);
```

**주의**: `const` → `final` 변경 필수

---

### 📊 Phase 1 결과

#### Before vs After

| 플랫폼            | Before                   | After                    |
| ----------------- | ------------------------ | ------------------------ |
| **안드로이드**    | ❌ localhost → 접속 불가 | ✅ 10.0.2.2 자동 사용    |
| **iOS/데스크톱**  | ✅ localhost 작동        | ✅ localhost 유지        |
| **코드 일관성**   | ❌ 파일마다 다름         | ✅ PlatformDefaults 통일 |
| **.env 우선순위** | ✅ 유지                  | ✅ 유지                  |

#### 실행 검증 ✅

```log
I/flutter (31371): uri: http://10.0.2.2:8080/api/groups
I/flutter (31371): statusCode: 200
```

→ 안드로이드 에뮬레이터에서 자동으로 `10.0.2.2` 사용 확인!

---

## Phase 2: 에러 핸들링 강화

### 📊 작업 개요

**날짜**: 2025-11-27
**목표**: 디버깅 효율성 + 사용자 경험 향상
**결과**: 명확한 예외 발생 + 사용자 안내

### 🏗️ 에러 처리 아키텍처

```
┌──────────┐
│  서버    │ → success: false, message: "비즈니스 에러"
└────┬─────┘
     │
     ↓
┌──────────────────┐
│  Repository      │ → Exception 발생 (서버 메시지 포함)
│  - uploadBatch   │ → FormatException (파싱 오류)
└────┬─────────────┘
     │
     ↓
┌──────────────────┐
│  UI              │ → SnackBar 표시
│  - live_map_page │ → debugPrint 로깅
│                  │ → 상태 복구
└──────────────────┘
```

**책임 분리**:

- **서버**: 비즈니스 로직 검증 + 메시지 제공
- **Repository**: 기술적 검증 + 예외 발생
- **UI**: 사용자 안내 + 상태 관리

---

### 1️⃣ TrackUploadResult.fromJson 개선

#### `lib/domain/tracking/tracking_dto.dart` 🔧

```dart
// Before
factory TrackUploadResult.fromJson(dynamic json) {
  if (json is Map && json['value'] is num) {
    return TrackUploadResult((json['value'] as num).toInt());
  }
  if (json is num) {
    return TrackUploadResult(json.toInt());
  }
  return const TrackUploadResult(0);  // ❌ 조용히 실패
}

// After
factory TrackUploadResult.fromJson(dynamic json) {
  if (json is Map && json['value'] is num) {
    return TrackUploadResult((json['value'] as num).toInt());
  }
  if (json is num) {
    return TrackUploadResult(json.toInt());
  }

  // 예상치 못한 형식에 대해 명확한 예외 발생 (디버깅 효율성 향상)
  throw FormatException(
    'TrackUploadResult 파싱 실패. '
    '예상: Map{"value": num} 또는 num, '
    '실제: ${json.runtimeType} - $json',
  );
}
```

**효과**:

- ✅ 잘못된 JSON 즉시 감지
- ✅ 명확한 에러 메시지
- ✅ 서버 응답 변경 시 즉시 인지

---

### 2️⃣ tracking_repository 공용 파서 적용

#### `lib/domain/tracking/tracking_repository.dart` 🔧

```dart
// Before
Future<TrackUploadResult> uploadBatch(TrackBatchRequest req) async {
  final resp = await _dio.post('/track/tracks/bulk', data: req.toJson());
  final data = resp.data is Map ? (resp.data['data'] ?? resp.data) : resp.data;
  return TrackUploadResult.fromJson(data);
}

// After
import 'package:oath_client/common/api_response.dart';

Future<TrackUploadResult> uploadBatch(TrackBatchRequest req) async {
  final resp = await _dio.post('/track/tracks/bulk', data: req.toJson());

  // 1. 응답 타입 검증
  if (resp.data is! Map<String, dynamic>) {
    throw FormatException(
      '서버 응답 형식 오류: Map 예상, ${resp.data.runtimeType} 수신',
    );
  }

  // 2. 공용 ApiResponse 파서 사용
  final apiResp = ApiResponse.fromJson(
    resp.data as Map<String, dynamic>,
    (json) => TrackUploadResult.fromJson(json),
  );

  // 3. 서버 success 필드 검증
  if (!apiResp.success) {
    throw Exception(
      '위치 업로드 실패: ${apiResp.message}', // 서버 메시지 활용
    );
  }

  // 4. data null 체크
  if (apiResp.data == null) {
    throw Exception('위치 업로드 실패: 응답 데이터 없음');
  }

  return apiResp.data!;
}
```

**효과**:

- ✅ 코드 일관성 (domain.members와 동일 패턴)
- ✅ success 필드 검증
- ✅ 서버 메시지 활용
- ✅ 유지보수성 향상

---

### 3️⃣ live_map_page UI 에러 처리

#### `lib/view/live_map/live_map_page.dart` 🔧

```dart
// Before
try {
  await repo.upload(dto);
  // ...
} catch (_) {
  // ❌ 에러 정보 무시
  _stopUploadLoop();
  // ...
}

// After
try {
  await repo.upload(dto);

  _updateMyMarker(here, ts: now.toLocal());
  _touchPresence(_myMemberId ?? -1);
  _sendPresence('ping');
} catch (e) {
  // 로깅 (개발 중 디버깅용)
  debugPrint('[위치 업로드 실패] $e');

  // 사용자에게 안내
  if (mounted) {
    ScaffoldMessenger.of(context).showSnackBar(
      const SnackBar(
        content: Text('위치 공유 중 오류가 발생했습니다'),
        duration: Duration(seconds: 2),
      ),
    );
  }

  // 상태 복구
  _stopUploadLoop();
  if (mounted) {
    setState(() {
      _shareMyLocation = false;
      _myAlpha = 0;
    });
  }
  _removeMyMarkerImmediate();
  _sendPresence('leave');
}
```

**효과**:

- ✅ 사용자 안내 SnackBar
- ✅ debugPrint 로깅
- ✅ 에러 정보 보존

---

### 📊 Phase 2 결과

#### Before vs After

| 항목               | Before                  | After                      |
| ------------------ | ----------------------- | -------------------------- |
| **JSON 파싱 오류** | ❌ TrackUploadResult(0) | ✅ FormatException 발생    |
| **서버 에러**      | ❌ 무시                 | ✅ Exception + 서버 메시지 |
| **사용자 안내**    | ❌ 없음                 | ✅ SnackBar "오류 발생"    |
| **개발 로그**      | ❌ 없음                 | ✅ debugPrint 상세 정보    |
| **코드 일관성**    | ❌ 커스텀 파싱          | ✅ ApiResponse 파서        |

#### 테스트 시나리오

**✅ 정상 케이스**

```
서버: {"success": true, "data": {"value": 5}, "message": "성공"}
→ TrackUploadResult(5) 반환
```

**⚠️ 서버 비즈니스 에러**

```
서버: {"success": false, "message": "위치 정보가 유효하지 않습니다"}
→ Exception 발생 + 사용자 SnackBar 표시
```

**🐛 잘못된 JSON 형식**

```
서버: {"success": true, "data": {"count": 5}}  ← value 대신 count
→ FormatException 발생 + debugPrint 로그
→ 개발자: 로그에서 즉시 문제 파악
```

---

## Phase 3: 구조 개선 (replaceAll 제거)

### 📊 작업 개요

**날짜**: 2025-11-27
**목표**: endpoints.dart replaceAll 불안정성 제거
**결과**: HOST 기반 구조로 변경

### 문제점

```dart
// Before - replaceAll 사용
String get _baseApi =>
    dotenv.env['API_BASE_URL']?.replaceAll('/api', '') ?? _fallbackApi;
String get _baseAi =>
    dotenv.env['AI_BASE_URL']?.replaceAll('/metrics', '') ?? _fallbackAi;
```

**문제**:

- `.env` URL 형식에 대한 가정 (특정 경로로 끝남)
- `/api/v1` 같은 경로 시 의도와 다르게 동작
- 설정 값 형식 의존성 높음

---

### 해결 방안: HOST 기반 구조

#### 1️⃣ `.env.example` 변경

```env
# Before
API_BASE_URL=http://10.0.2.2:8080/api
AI_BASE_URL=http://192.168.0.3:8001/metrics

# After
API_HOST=http://10.0.2.2:8080               # 경로 제외
AI_HOST=http://192.168.0.3:8001             # 경로 제외
```

#### 2️⃣ `endpoints.dart` 리팩터링

```dart
// Before
String get _fallbackApi => PlatformDefaults.httpImage;
String get _fallbackAi => PlatformDefaults.aiBase;

String get _baseApi =>
    dotenv.env['API_BASE_URL']?.replaceAll('/api', '') ?? _fallbackApi;
String get _baseAi =>
    dotenv.env['AI_BASE_URL']?.replaceAll('/metrics', '') ?? _fallbackAi;

class Endpoints {
  static String rollupPlan(int planId) =>
      '$_baseApi/api/plans/$planId/metrics/rollup';
  static String report(int planId) => '$_baseAi/metrics/report/$planId';
}

// After
String get _apiHost => dotenv.env['API_HOST'] ?? PlatformDefaults.httpImage;
String get _aiHost => dotenv.env['AI_HOST'] ?? PlatformDefaults.aiBase;

class Endpoints {
  // Spring Boot API
  static String rollupPlan(int planId) =>
      '$_apiHost/api/plans/$planId/metrics/rollup';

  // FastAPI
  static String report(int planId) => '$_aiHost/metrics/report/$planId';
}
```

---

### 📊 Phase 3 결과

#### Before vs After

| 항목            | Before                 | After                      |
| --------------- | ---------------------- | -------------------------- |
| **.env 구조**   | `API_BASE_URL=.../api` | `API_HOST=...` (경로 제외) |
| **replaceAll**  | ✅ 사용 (불안정)       | ❌ 제거                    |
| **경로 조합**   | ❌ .env 의존           | ✅ 코드에서 명시           |
| **형식 의존성** | ❌ 높음                | ✅ 없음                    |

#### 효과

- ✅ **명확성**: HOST와 경로가 분리되어 의도 명확
- ✅ **안정성**: 형식 가정 제거
- ✅ **확장성**: 미래 경로 변경 시 코드만 수정

---

### ⚠️ 마이그레이션 가이드

**기존 .env 파일 사용자:**

```bash
# 기존
API_BASE_URL=http://10.0.2.2:8080/api
AI_BASE_URL=http://192.168.0.3:8001/metrics

# 변경 필요
API_HOST=http://10.0.2.2:8080
AI_HOST=http://192.168.0.3:8001
```

**PowerShell 자동 변환 (선택):**

```powershell
(Get-Content .env) -replace 'API_BASE_URL=(.+)/api', 'API_HOST=$1' | Set-Content .env
(Get-Content .env) -replace 'AI_BASE_URL=(.+)/metrics', 'AI_HOST=$1' | Set-Content .env
```

---

## 전체 변경 요약

### 📊 통계

```
신규:    1 file   (+29 lines)
수정:    8 files  (~85 lines changed)

Phase 1: 4 files (1 신규, 3 수정)
Phase 2: 3 files (모두 수정)
Phase 3: 2 files (모두 수정)
```

### 📝 변경 파일 목록

#### 신규 파일 (1개)

- ✨ `lib/common/utils/platform_defaults.dart`

#### 수정 파일 (8개)

**Phase 1 (3개)**

- 🔧 `lib/common/utils/http_util.dart`
- 🔧 `lib/common/api/endpoints.dart`
- 🔧 `lib/view/live_map/live_map_page.dart` (WebSocket)

**Phase 2 (3개)**

- 🔧 `lib/domain/tracking/tracking_dto.dart`
- 🔧 `lib/domain/tracking/tracking_repository.dart`
- 🔧 `lib/view/live_map/live_map_page.dart` (에러 처리)

**Phase 3 (2개)**

- 🔧 `.env.example`
- 🔧 `lib/common/api/endpoints.dart`

---

## 검증 및 테스트

### ✅ Phase 1 검증 완료

- [x] 안드로이드 에뮬레이터에서 API 호출 성공 (10.0.2.2 자동 사용)
- [x] .env 설정 우선순위 유지 확인
- [ ] iOS 시뮬레이터 테스트 (권장)
- [ ] WebSocket 연결 테스트 (권장)

### 🔜 Phase 2-3 검증 권장

- [ ] 정상 케이스: 위치 업로드 성공 확인
- [ ] 네트워크 오류: SnackBar 표시 확인
- [ ] 잘못된 JSON: debugPrint 로그 확인
- [ ] 서버 에러: 서버 메시지 활용 확인
- [ ] .env 마이그레이션 후 API 호출 테스트

---

## 💡 핵심 성과

### 1️⃣ 개발자 경험 향상

- ✅ Platform 자동 감지 (수동 설정 불필요)
- ✅ 명확한 에러 메시지 (빠른 디버깅)
- ✅ 코드 일관성 확보 (ApiResponse 패턴)

### 2️⃣ 사용자 경험 향상

- ✅ 안드로이드 에뮬레이터 정상 작동
- ✅ 에러 시 명확한 안내
- ✅ 자동 상태 복구

### 3️⃣ 코드 품질 향상

- ✅ 중복 코드 제거
- ✅ 명확한 책임 분리 (서버/Repository/UI)
- ✅ 미래 확장성 확보

---

## 🔗 관련 문서

- [1126\_컨텍스트문서.md](file:///c:/workspace_flutter/oathkeeper_front/docs/choong/1126_컨텍스트문서.md)
- [LIVE_MAP_API.md](file:///c:/workspace_flutter/oathkeeper_front/docs/LIVE_MAP_API.md)
- [api_response.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/common/api_response.dart)

### 코드 파일

- [platform_defaults.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/common/utils/platform_defaults.dart)
- [http_util.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/common/utils/http_util.dart)
- [endpoints.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/common/api/endpoints.dart)
- [tracking_dto.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/domain/tracking/tracking_dto.dart)
- [tracking_repository.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/domain/tracking/tracking_repository.dart)
- [live_map_page.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/view/live_map/live_map_page.dart)

---

**문서 종료**
최종 업데이트: 2025-11-27 15:01
