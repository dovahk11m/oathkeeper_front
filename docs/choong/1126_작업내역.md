# 1126 작업 내역

## 주요 변경
- 환경변수 적용
  - `main.dart`에 `.env` 로드 추가.
  - `http_util.dart`, `ai_api.dart`, `endpoints.dart`를 .env 우선 + 로컬 fallback 구조로 정리.
  - `.env.example` 갱신 및 설명 문서 추가(`docs/choong/1126_env_정리.md`).
- 라이브맵/공용 정리
  - `tracking_dto/repository/provider`를 배치 업로드 DTO 기준으로 재정비(deprecated는 남김).
  - `live_map_page.dart` WS 기본값을 localhost로 정리(WS_URL env 우선).
- 문서 업데이트
  - `1126_컨텍스트문서.md`, `1126_공용파일리팩토링작업.md`, `1126_SERVER_CLIENT_QNA.md` UTF-8 재작성 및 요청/답변 보강.
  - 공용파일 트리(`lib/common/공용파일트리.md`) 작성.

## 미해결/리스크
- `websocket_service.dart`는 여전히 채팅 전용 구 경로(요건 분리/리팩터 필요).
- LiveMap UI: 그룹 통계 조회는 성공 로그만 보이고 화면 반영 안 됨(뷰 로직 확인 필요).
- env 키 정리 필요(불필요 키 최소화 검토).


# 1127 작업 계획

## 즉시 할 일
- LiveMap 뷰 반영 확인: 그룹/플랜 요약 성공 응답이 UI에 표시되지 않는 문제 확인 및 수정.
- WebSocket 정리: 채팅/라이브맵 WS 경로 분리 여부 결정, `websocket_service.dart` 환경분기/경로 파라미터화.
- env 키 최소화: 실제 사용하는 키만 남기고 문서/예시 정리.

## 진행/검토
- tracking 도메인/뷰 교체: batch 업로드(POST /track/tracks/bulk), /topic/plans/{planId}/live|events 토픽 적용 리팩터 준비.
- Group/Plan 스키마 재확인: 서버 스키마 불일치/500 이슈 해소 확인 후 파서 점검.

## 서버 커뮤니케이션
- `1126_SERVER_CLIENT_QNA.md`에 따라 WS 경로/토픽/JWT 유지 여부 최종 확정 요청.
- 스키마 변경 시 버전 명시 및 공지 요청 유지.
