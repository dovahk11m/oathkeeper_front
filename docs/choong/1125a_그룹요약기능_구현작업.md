# 1125a 그룹 요약 기능 구현 작업

**작업일**: 2025-11-25

## 📋 구현 내용

### 1. Domain Layer 추가

#### 그룹 통계 DTO

- **파일**: `lib/domain/metrics/group_metrics_summary.dart`
- `GroupSummaryData`: 그룹 누적 통계 세부 정보
- `GroupMetricsSummary`: API 응답 데이터 구조

#### 상태 관리

- **파일**: `lib/domain/metrics/group_metrics_state.dart`
- 로딩, 데이터, 에러 상태 관리

#### Provider

- **파일**: `lib/domain/metrics/group_metrics_provider.dart`
- `GroupMetricsNotifier`: 그룹 통계 조회 로직
- `fetchGroupSummary(int groupId)`: API 호출 메서드

#### Repository 개선

- **파일**: `lib/domain/metrics/metrics_repository.dart`
- `fetchGroupSummary()`: 그룹 누적 통계 조회 메서드 추가

- **파일**: `lib/domain/groups/group_repository.dart`
- `fetchGroupPlans()`: status 파라미터를 옵셔널로 변경 (모든 약속 조회 가능)

- **파일**: `lib/domain/plans/simple_plan.dart`
- `status` 필드 추가 (약속 상태 구분)

### 2. View Layer 파일 분리

기존 650줄의 `metrics_summary_sheet.dart`를 4개 파일로 분리:

#### 메인 파일

**파일**: [metrics_summary_sheet.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/view/metrics/metrics_summary_sheet.dart)

- 탭 컨트롤러 및 탭 전환 로직만 담당 (~140줄)
- 각 탭은 별도 위젯으로 분리

#### 그룹 통계 탭

**파일**: [group_stats_tab.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/view/metrics/widgets/group_stats_tab.dart)

- 그룹 전체 누적 통계 표시
- 분석된 약속 수, 총/평균 이동 거리, 총/평균 지각 시간
- AI 자연어 요약

#### 약속 목록 탭

**파일**: [plan_list_tab.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/view/metrics/widgets/plan_list_tab.dart)

- 그룹 내 모든 약속 표시 (진행 중 + 완료)
- 완료된 약속: 클릭 가능 (개별 요약 표시)
- 진행 중 약속: 비활성화 상태로 표시

#### 개별 약속 요약

**파일**: [individual_summary_sheet.dart](file:///c:/workspace_flutter/oathkeeper_front/lib/view/metrics/widgets/individual_summary_sheet.dart)

- DraggableScrollableSheet으로 구현
- 멤버 목록 및 AI 요약 표시
- 폴링 방식으로 요약 로딩

### 3. API 연동

#### 그룹 누적 통계

```
GET /api/groups/{groupId}/metrics/summary
```

- 그룹 전체 통계 및 AI 요약 조회

#### 약속 목록

```
GET /api/groups/{groupId}/plans?status=COMPLETED
```

- status 파라미터 옵셔널 (생략 시 모든 약속)

#### 개별 약속 요약

```
GET /api/plans/{planId}/summary
```

- 폴링 방식으로 AI 요약 조회

## 🎯 주요 개선사항

### 코드 품질

- ✅ 파일 분리로 관리 용이성 향상 (650줄 → 140줄)
- ✅ 각 위젯의 책임 명확화
- ✅ 재사용성 증가

### 기능

- ✅ 그룹 전체 통계 표시
- ✅ 진행 중/완료 약속 모두 표시
- ✅ 진행 중 약속은 비활성화 (UX 향상)
- ✅ 개별 약속 요약 바텀시트

### 상태 관리

- ✅ Riverpod Notifier 패턴 일관성 유지
- ✅ 모든 데이터 fetching을 Provider로 관리

## 📚 참고 문서

- [METRIC_API.md](file:///c:/workspace_flutter/oathkeeper_front/docs/METRIC_API.md)
- [GROUP_API.md](file:///c:/workspace_flutter/oathkeeper_front/docs/GROUP_API.md)
- [작업 계획](file:///c:/workspace_flutter/oathkeeper_front/docs/choong/1125_todo.md)

## 🔍 구현 세부사항

### Domain Layer

```dart
// GroupMetricsSummary DTO
@freezed
class GroupMetricsSummary with _$GroupMetricsSummary {
  const factory GroupMetricsSummary({
    @JsonKey(name: 'group_summary') GroupSummaryData? groupSummary,
    @JsonKey(name: 'text_summary') String? textSummary,
    String? message,
  }) = _GroupMetricsSummary;

  factory GroupMetricsSummary.fromJson(Map<String, dynamic> json) =>
      _$GroupMetricsSummaryFromJson(json);
}

// GroupMetricsProvider
class GroupMetricsNotifier extends Notifier<GroupMetricsState> {
  Future<void> fetchGroupSummary(int groupId) async {
    // API 호출 및 상태 업데이트
  }
}
```

### View Layer 구조

자세한 파일 구조는 [매트릭스뷰\_구조.md](file:///c:/workspace_flutter/oathkeeper_front/lib/view/metrics/매트릭스뷰_구조.md)를 참고하세요.

## ✅ 체크리스트

- [x] Domain Layer DTO, State, Provider 구현
- [x] Repository API 메서드 추가
- [x] View Layer 파일 분리 (4개 파일)
- [x] 그룹 통계 탭 UI 구현
- [x] 약속 목록 탭 UI 구현
- [x] 개별 약속 요약 바텀시트 구현
- [x] 빌드 확인
- [ ] 기능 테스트 (사용자 확인 필요)

## 🐛 알려진 이슈

- flutter analyze에서 deprecated warning 6개 (기존 코드 관련, 기능에 영향 없음)
