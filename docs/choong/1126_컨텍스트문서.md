# Current Problem Context (2025-11-26)

- 목적: 새 작업자가 규칙·최근 이슈·목표·주요 경로를 한눈에 파악해 바로 작업을 이어받도록 돕는다.

## 0. 작업 규칙 및 코드 규칙
1) 모든 커뮤니케이션은 텍스트로만 진행.
2) 모든 답변 시 생각과정과 변경내역을 포함하여 한국어로 제공.
3) 프런트 작업 시 불필요한 네트워크/패키지 설치 지양, 테스트는 가능 범위 내에서.

## 1. 최근 상태 요약
- AI 요약/메트릭스
  - 1124~1125: Python 직접 호출 → Spring API 일원화, 3초 폴링, 404/409/500 처리, Freezed DTO/State/Provider 완료.
  - MetricsSummarySheet를 4개 파일로 분리(메인/그룹 통계/플랜 목록/개별 요약), 그룹 메트릭스 도입.
  - 서버 이슈: 그룹 플랜 목록 스키마 불일치 및 일부 500 있었음(문서 최신본과 일치 여부는 서버가 최종 확인).
- 라이브맵
  - 1121 기록: GPS 정지 감지, 도착 감지, MovementStatus 추가, WebSocket 알림 구현(서버).
  - 현 클라 코드는 구 명세 기반(room 토픽, 하드코딩 IP); 최신 LIVE_MAP_API와 불일치.

## 2. 다음 목표
1) 라이브맵: LIVE_MAP_API 기준으로 REST/WS 엔드포인트 교체, 이벤트 처리, 업로드 루프/재연결/테스트 API(local) 반영.
2) AI 요약/메트릭스: 그룹·플랜 요약 UI 통합 테스트 및 서버 연동 재확인.
3) 서버 연동 이슈 대응: 플랜 목록 스키마/500 오류 해소 확인 후 모델/파서 재검증.

## 3. 장기 목표
- AI 요약/메트릭스 안정화(스키마 일치, 폴링 UX 안정).
- 라이브맵 실시간 UX 완성(도착/정지 이벤트 반영, 알림/상태 표시).
- 그룹 단위 통계/요약 고도화(1차 구현 완료, 품질 검증/개선 필요).

## 4. 주요 문서/파일 경로
- 문서: docs/METRIC_API.md, docs/PLACE_API.md, docs/LIVE_MAP_API.md, docs/choong/1124_todo.md, docs/choong/1125_todo.md, docs/choong/1124i_서버_API_응답_불일치_리포트.md, docs/1126_JAVA_SERVER_REQ.md, docs/1126_SERVER_CLIENT_QNA.md
- 라이브맵 관련 문서: docs/choong/1126_실시간지도구현계획.md, docs/choong/1121e_실시간지도_보강계획.md, docs/choong/1121f_실시간지도_작업기록.md
- 컨텍스트 예시: docs/choong/1126_CurrentContext.md
- 메트릭스 도메인/상태: lib/domain/metrics/plan_summary.dart, .../metrics_state.dart, .../metrics_provider.dart, .../metrics_repository.dart; group_metrics_summary.dart, .../group_metrics_state.dart, .../group_metrics_provider.dart; lib/domain/plans/simple_plan.dart, plan_list_state.dart, plan_list_provider.dart
- 메트릭스 뷰: lib/view/metrics/metrics_summary_sheet.dart; lib/view/metrics/widgets/group_stats_tab.dart; plan_list_tab.dart; individual_summary_sheet.dart
- 라이브맵 코드 위치: lib/domain/tracking/ (tracking_dto/repository/provider), lib/view/live_map/ (live_map_page.dart 등)

## 5. 라이브맵 현황 및 과제
- 현재 코드
  - 도메인: lib/domain/tracking/tracking_dto.dart / tracking_repository.dart / tracking_provider.dart — 하드코딩 IP(`http://192.168.0.187`), 구 REST(`/api/location/update`, `/api/location/recent`), 커스텀 dioWithAuth.
  - 뷰: lib/view/live_map/live_map_page.dart — STOMP `/topic/room/{planId}` + presence 전용 토픽, 커스텀 WS URL env `WS_URL`, 이벤트 처리 없음.
- 서버 최신 명세(LIVE_MAP_API & QnA)
  - WS: `/ws-stomp`(SockJS)/`/ws`, 구독 `/topic/plans/{planId}/live`, `/topic/plans/{planId}/events`, JWT 필수.
  - 업로드: `POST /api/track/tracks/bulk` (JWT), 3~5초 배치 권장.
  - 이벤트: GPS_STATIONARY, PARTICIPANT_ARRIVED 알림 수신.
  - 테스트 API: force-movement/force-stationary/force-arrived는 local 프로파일에서만 활성.
- 해야 할 일
  - tracking repo/provider를 새 REST 엔드포인트로 교체(dioProvider 재사용, bulk DTO).
  - WS 헬퍼/구독을 새 토픽으로 전환, 재연결 백오프, 이벤트 처리 추가.
  - 기존 live_map_page 리팩터(legacy 코드 정리, presence 처리 재검토).
  - 테스트 API는 local에서만 스모크용으로 호출.
- 환경 변수 정리 참고: docs/choong/1126_env_정리.md (하드코딩 IP 제거, .env 우선 구조 설명)

## 6. 프런트 개발환경 & domain.members 컨벤션
- 빌드/런타임: Android Gradle(`android/build.gradle`) google/mavenCentral, Java 17; Flutter SDK(`pubspec.yaml`) >=3.4.0<4.0.0, 주요 의존성 dio 5.x, flutter_riverpod 2.5, freezed/json_serializable, go_router 16.1.x, flutter_secure_storage, geolocator/permission_handler, kakao/flutter_facebook_auth 등. Assets: Pretendard 폰트, assets/images/logo.jpg, .env.
- domain.members: 배럴(lib/domain/members/member.dart)로 export; 서브모듈 auth/signup/find_account/profile/password/term 각각 DTO(@freezed + fromJson), state(@freezed), Provider(Notifier/NotifierProvider), dioProvider/FlutterSecureStorage 주입, ApiResponse<T> 파서. 로그인은 전략/어댑터(Kakao/Facebook) 사용.
- 공용 네트워크/WS 설정 참고: `lib/common/utils/http_util.dart` 기본 baseUrl=10.0.2.2 (로컬), `lib/common/api/ai_api.dart`는 192.168.0.3:8001 LLM용(legacy), `lib/common/websocket_service.dart`는 chat room용 구 WS 경로(ws://10.0.2.2/ws) 사용 중 — 라이브맵/신규 WS 리팩터 시 교체 필요.
- .env/.env.example 키: `API_BASE_URL`, `IMAGE_BASE_URL`, `AI_BASE_URL`, `WS_CHAT_URL`, `WS_LIVEMAP_URL` (dev/prod 분리 키 포함). 현재 common 코드가 이 키를 읽도록 변경됨.
