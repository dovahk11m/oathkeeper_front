# 1126 공용파일 리팩토링 작업 메모

## TODO 체크리스트
- [x] http_util.dart: baseUrl 고정 해소, .env 기반 주입으로 전환 (fallback localhost 유지)
- [x] api/ai_api.dart, api/endpoints.dart: 고정 IP → .env 주입, 불필요 시 분리 검토
- [ ] websocket_service.dart: 구 chat room 경로(ws://10.0.2.2/ws 등) → 환경 분기/분리 필요
- [ ] api_response.dart, router.dart, token_interceptor.dart: 치명 문제 없음. ApiResponse null 가드, router 경로 증가 시 점검 예정

## 작업 요약
- 네트워크 설정을 .env 우선 + 로컬 기본값(fallback) 구조로 통일
  - `lib/common/utils/http_util.dart`: `API_BASE_URL`, `IMAGE_BASE_URL`을 dotenv로 읽고, 없으면 `http://localhost:8080` 사용.
  - `lib/common/api/ai_api.dart`: `AI_BASE_URL` 우선, 없으면 `http://192.168.0.3:8001/metrics` 기본.
  - `lib/common/api/endpoints.dart`: `API_BASE_URL`/`AI_BASE_URL` 기반으로 엔드포인트 빌드, fallback은 localhost/192.168.0.3.
  - `lib/view/live_map/live_map_page.dart`: `WS_URL` 없으면 `ws://localhost:8080/ws` 기본값 사용.
- 환경 변수 예시 업데이트
  - `.env.example`에 API/IMAGE/AI/WS 키 예시 및 사용처 주석 추가. AI 서버 변경 시 `AI_BASE_URL`만 교체하면 됨.
- 변경 이유 문서화
  - `docs/choong/1126_env_정리.md`: 하드코딩 IP 제거, .env 주입 구조, 사용법/주의사항 정리.

## 참고 경로
- 공용 트리: `lib/common/공용파일트리.md`
- 컨텍스트: `docs/choong/1126_컨텍스트문서.md`
- 환경 정리: `docs/choong/1126_env_정리.md`
