# 1127 작업 내역

## 완료된 작업

### Phase 1: Platform 호환성 확보

- [x] `.env` 구조 HOST 기반으로 변경
- [x] `platform_defaults.dart` 생성 (Android 에뮬레이터 대응)
- [x] `endpoints.dart` replaceAll 제거

### Phase 3: 실시간 지도 기능 개선

- [x] 현황 분석 및 서버 스펙 확인 (Q&A 완료)
- [x] `LiveLocationDto` 생성
- [x] WebSocket 토픽 변경 (`room` -> `plans`)
- [x] 이벤트 처리 로직 구현 (`GPS_STATIONARY`, `PARTICIPANT_ARRIVED`)
- [x] Presence 로직 제거 및 코드 정리
- [x] 테스트 및 검증 (빌드 성공)
- [x] 작업 내역 문서화 (`1127d_실시간지도리팩토링작업.md`)

### Phase 4: 버그 수정 및 안정화

- [x] 약속 생성 시 `groupId` 누락 수정 (500 에러 해결)
  - [x] `PlanRepository.createPlan`에 `groupId` 추가
  - [x] `PlanProvider.createPlanWithParticipants`에 `groupId` 추가
  - [x] `CreatePlanScreen`에서 `_selectedGroupId` 전달
- [x] IDE 경고 수정 (JsonKey, group_provider)
- [x] 약속 목록 UI 버그 수정 (완료된 약속 미완료 표시 해결)
  - [x] `SimplePlan`에 `isCompleted` getter 추가 (대소문자 무시)
  - [x] `PlanListTab`에서 `isCompleted` 사용하도록 수정

## 발견된 이슈 (서버 조치 필요)

### 약속 목록 조회 API `status` 필드 누락

- **문서**: `1127e_약속요약기능_QNA.md` 참고
- **현상**: DB에는 `COMPLETED` 상태가 정상 저장되어 있으나, 클라이언트에서 미완료로 표시
- **원인**: `GET /api/groups/{groupId}/plans` API 응답에 `status` 필드가 포함되지 않음
- **조치 요청**: 서버팀에 응답 DTO에 `status` 필드 추가 요청

## 생성된 문서

- `docs/choong/1127a_클라이언트연동지원계획.md`
- `docs/choong/1127b_PR리뷰반영리팩토링문서.md`
- `docs/choong/1127c_실시간지도_QNA.md`
- `docs/choong/1127d_실시간지도리팩토링작업.md`
- `docs/choong/1127e_약속요약기능_QNA.md`
